{
  "scripts": {
    "dev": "vite --host 0.0.0.0 --https",
    "build": "vite build",
    "lint": "eslint . --ext js,jsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview --host 0.0.0.0",
    "deploy": "npm run build && npx serve dist"
  }
}
